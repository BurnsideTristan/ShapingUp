using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class AllCollected : MonoBehaviour
{
   /* public string NextLevel;
    public int NumberOfCollectibles;
    public GameObject player;
    private int collectedItems;

    private void Start()
    {
        collectedItems = player.GetComponent<PlayerMovement>().getCount();
        // SceneManager.LoadScene(NextLevel, LoadSceneMode.Additive);

    }

    private void OnTriggerEnter(Collider other)
    {
        if (other.GetComponent<PlayerMovement>().getCount() == NumberOfCollectibles)
        {
            //enter new scene and make counter == 0
            SceneManager.LoadScene(NextLevel);
            other.GetComponent<PlayerMovement>().setCount(0);
        }
    }
    */
}
